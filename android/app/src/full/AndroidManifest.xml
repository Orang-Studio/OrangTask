<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>

        <!-- Firebase Cloud Messaging: receives push, builds notifications with
             action buttons (Mark done / Snooze) in OrangMessagingService. -->
        <service
            android:name=".notifications.OrangMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/fcm_default_channel" />
    </application>
</manifest>
